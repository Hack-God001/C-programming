#include <stdio.h>

int main(){
	printf("Finding the number of days in a month \n\n\n");
	float x=7; //x=1 week;
	float y=4.4;  //y = 4.4 weeks
	int days;
	
	days = (y*x);
	
	printf("there are %d days in the month", days);
}
